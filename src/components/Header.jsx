import React from 'react';
import '../styles/Header.css';

const Header = () => {
    return (
        <header className="app-header">
            <h1>TickTask</h1>
        </header>
    );
};

export default Header;